Explore the existing code base. Write documentation into docs/*.md.

Cover:

architecture (subsystem map and how they share the simulation engine),
technologies (per subsystem),
dependencies (incl. versions pinned from actual config files),
build & run workflows (per subsystem, incl. prerequisites),
project lineage and current status.
Game design, game UI, gameplay, flows, ...
etc.
Flag anything outdated or broken. Use ASCII-art diagrams for architecture and data flow. Read existing documentation before writing - incorporate, don't duplicate.

Assume the readers are technical, but not necessarily experts for the (old) technologies, so always add additional explanations.