# ── Build artifacts ───────────────────────────────────────────────
target/
rust_out/
.DS_Store
# ── Shipping binaries — distribute via GitHub Releases, not in tree

*.pkg
*.zip
*.tar.gz
*.app

# ── Test / sample data may contain real ClOrdIDs / account numbers
fixtures/
*.log
*.fix
sample_*.fix
*.fix.zip
.DS_Store

# ── Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
*~

# ── Large demo media — keep locally, never commit
demo/*.mov
demo/*.mp4

# ── Local recents store (just in case)
.aifixparser/
